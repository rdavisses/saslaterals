This specific configuration of files is designed to be run from a folder downloaded and extracted directly to user desktop, pulling the python environment path from nuwave. This is highly specific and may be need to modifed for other use cases, but this was done as the current setup at SAS (as of 7/12/24) is very non conducive to on the fly modifications. 